// when the documents is ready -> functions
$(document).ready(function(){
    $('.burger-menu').on('click', function(){
        $('.mobile-view').toggle("fast")
    })
}
)

// toggle -> inverte o CSS (.mobile-view)

// jQuery = $